// import personSetup from './Person.ts';

export function setup (core:any) {
	// Init definitoins
		let services:any = {};

	// Setup services
		// personSetup(services);

	// Export services
		core.services = services;
}